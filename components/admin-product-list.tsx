import { deleteVariant, saveCategory, saveProduct, saveVariant, setCategoryActive, setProductAvailability, uploadProductImage } from "@/app/admin/actions";

type Category = { id: string; name: string; slug?: string; description?: string | null; is_active: boolean };
type Variant = { id: string; name: string; size: string | null; price: number; original_price: number | null };
type Product = { id: string; name: string; slug: string; description: string | null; featured: boolean; is_available: boolean; category_id: string; categories: { name: string } | { name: string }[] | null; product_variants: Variant[] };

export function AdminProductList({ categories, products }: { categories: Category[]; products: Product[] }) {
  return <>
    <section className="admin-section">
      <div className="admin-section-heading"><div><p className="eyebrow">Catalog controls</p><h2>Categories</h2></div><span>{categories.length} total</span></div>
      <form action={saveCategory} className="admin-edit-form"><input name="id" type="hidden" value="" /><label>Name<input name="name" required /></label><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required /></label><button className="button-primary" type="submit">Add category</button></form>
      <div className="admin-category-list">{categories.map((category) => <div className="admin-edit-card" key={category.id}><form action={saveCategory} className="admin-edit-form"><input name="id" type="hidden" value={category.id} /><label>Name<input defaultValue={category.name} name="name" required /></label><label>Slug<input defaultValue={category.slug ?? category.name.toLowerCase().replaceAll(" ", "-")} name="slug" required /></label><button className="button-secondary" type="submit">Save category</button></form><form action={setCategoryActive}>
        <input name="categoryId" type="hidden" value={category.id} /><input name="isActive" type="hidden" value={String(!category.is_active)} />
        <button className="status-button" type="submit">{category.is_active ? "Active" : "Hidden"}</button>
      </form></div>)}</div>
    </section>
    <section className="admin-section">
      <div className="admin-section-heading"><div><p className="eyebrow">Catalog controls</p><h2>Products</h2></div><span>{products.length} total</span></div>
      <form action={saveProduct} className="admin-edit-form admin-product-editor"><input name="id" type="hidden" value="" /><label>Name<input name="name" required /></label><label>Slug<input name="slug" required /></label><label>Category<select defaultValue={categories[0]?.id} name="categoryId">{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Description<textarea name="description" /></label><label><input name="featured" type="checkbox" /> Featured</label><label><input defaultChecked name="isAvailable" type="checkbox" /> Available</label><button className="button-primary" type="submit">Add product</button></form>
      <div className="admin-product-list">{products.map((product) => <div className="admin-edit-card" key={product.id}><form action={saveProduct} className="admin-edit-form admin-product-editor"><input name="id" type="hidden" value={product.id} /><label>Name<input defaultValue={product.name} name="name" required /></label><label>Slug<input defaultValue={product.slug} name="slug" required /></label><label>Category<select defaultValue={product.category_id} name="categoryId">{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Description<textarea defaultValue={product.description ?? ""} name="description" /></label><label><input defaultChecked={product.featured} name="featured" type="checkbox" /> Featured</label><label><input defaultChecked={product.is_available} name="isAvailable" type="checkbox" /> Available</label><button className="button-secondary" type="submit">Save product</button></form><form action={uploadProductImage} className="image-upload-form" encType="multipart/form-data"><input name="productId" type="hidden" value={product.id} /><label>Product image<input accept="image/jpeg,image/png,image/webp" name="image" required type="file" /></label><button className="status-button" type="submit">Upload image</button></form><div className="variant-editor"><strong>Variants</strong>{product.product_variants.map((variant) => <form action={saveVariant} className="variant-form" key={variant.id}><input name="id" type="hidden" value={variant.id} /><input name="productId" type="hidden" value={product.id} /><input aria-label="Variant name" defaultValue={variant.name} name="name" required /><input aria-label="Size" defaultValue={variant.size ?? ""} name="size" /><input aria-label="Price" defaultValue={variant.price} min="0" name="price" step="0.01" type="number" required /><input aria-label="Original price" defaultValue={variant.original_price ?? ""} min="0" name="originalPrice" step="0.01" type="number" /><button className="status-button" type="submit">Save</button><button className="status-button" formAction={deleteVariant} type="submit">Delete</button></form>)}<form action={saveVariant} className="variant-form"><input name="productId" type="hidden" value={product.id} /><input aria-label="New variant name" name="name" placeholder="New variant" required /><input aria-label="New variant size" name="size" placeholder="Size" /><input aria-label="New variant price" min="0" name="price" placeholder="Price" step="0.01" type="number" required /><input aria-label="New original price" min="0" name="originalPrice" placeholder="Original" step="0.01" type="number" /><button className="status-button" type="submit">Add variant</button></form></div><form action={setProductAvailability}><input name="productId" type="hidden" value={product.id} /><input name="isAvailable" type="hidden" value={String(!product.is_available)} /><button className="status-button" type="submit">{product.is_available ? "Available" : "Unavailable"}</button></form></div>)}</div>
    </section>
  </>;
}
